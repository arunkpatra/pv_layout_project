"""
pvlayout_engine — SolarLayout desktop sidecar.

This package wraps the PVlayout_Advance domain logic (copied into
`pvlayout_core/` in S1) in a FastAPI service (authored in S2), bound to
localhost and spawned by the Tauri desktop shell.

S0 stub: this package is intentionally empty. See docs/SPIKE_PLAN.md.
"""

__version__ = "0.0.0"
