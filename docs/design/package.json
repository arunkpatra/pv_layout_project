{
  "name": "@solarlayout/design-mocks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Static HTML design mocks for S5.5 — rendered to PNG via headless Chromium",
  "scripts": {
    "render": "node scripts/render-mocks.mjs",
    "build": "echo '[design-mocks] no build — static HTML'",
    "lint": "echo '[design-mocks] no lint yet'",
    "typecheck": "echo '[design-mocks] no typecheck'",
    "test": "echo '[design-mocks] no tests'"
  },
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
