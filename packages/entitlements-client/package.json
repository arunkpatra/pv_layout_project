{
  "name": "@solarlayout/entitlements-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Typed TypeScript client for api.solarlayout.in — GET /entitlements, POST /usage/report",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "echo '[entitlements-client] source-only; consumers transpile'",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.3"
  }
}
