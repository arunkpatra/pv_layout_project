# Gate — P0: Quick-Win Port + Discovery Memos

**Status:** Approved (Arun verbal signoff 2026-04-29)
**Spike:** P0 (parity track)
**Spec:** [`docs/parity/PLAN.md`](../parity/PLAN.md) §5 P0
**Plan:** [`docs/parity/plans/p00-quick-win-port.md`](../parity/plans/p00-quick-win-port.md)
**Started:** 2026-04-29
**Author:** Arun (review); Claude (drafting + execution)

---

## 1. Summary

P0 ports four functions from legacy `baseline-v1-20260429` (`_bundle_dc_cables`, `_build_mst_edges`, `_calc_individual_ac_total`, `_route_ac_mst`) into `pvlayout_core/core/string_inverter_manager.py`, restructures `place_string_inverters` to call them, and pre-files three discovery memos for Prasanta on the S11.5 additions (Pattern V, search-space caps, `route_quality`).

All S11.5 surface (Pattern V machinery, search-space caps, `route_quality` tagging, parameterised allowances on `LayoutParameters`, per-ICR/inverter subtotals on `LayoutResult`, `PVLAYOUT_PATTERN_STATS` instrumentation) is preserved. The port is additive: legacy's bundled / MST functions land on top of the S11.5 surface.

The crow's-feet visual symptom is resolved: AC cables now draw as MST trees, DC cables as row collectors + trunks per legacy.

---

## 2. What shipped

- `python/pvlayout_engine/scripts/parity/capture_legacy_baseline.py` — headless capture script.
- `docs/parity/baselines/baseline-v1-20260429/manifest.md` — baseline metadata, params used, deferral notes.
- `docs/parity/baselines/baseline-v1-20260429/ground-truth/phaseboundary2/numeric-baseline.json` — captured legacy numerics (phaseboundary2 only; complex-plant-layout deferred — see §5).
- `python/pvlayout_engine/tests/parity/test_p00_bundled_mst_parity.py` — parity regression tests (phaseboundary2 parametrized; complex-plant-layout explicitly excluded with comment).
- `python/pvlayout_engine/pvlayout_core/core/string_inverter_manager.py` — four ported functions + adapted `place_string_inverters`.
- `docs/parity/findings/2026-04-29-001-pattern-v.md` — Pattern V divergence memo.
- `docs/parity/findings/2026-04-29-002-search-space-caps.md` — search-space caps memo.
- `docs/parity/findings/2026-04-29-003-route-quality-field.md` — `route_quality` field memo.
- `docs/parity/BACKLOG.md` — cumulative drift enumeration beyond P0 scope.
- `CLAUDE.md` §2 reference to `docs/parity/PLAN.md`.
- `docs/SPIKE_PLAN.md` top-of-file parity callout.

---

## 3. What to run

### Static gates (sidecar)

```bash
cd /Users/arunkpatra/codebase/pv_layout_project/python/pvlayout_engine
uv run pytest tests/ -q
```

Expected: 72 passed, 6 skipped, 0 failed. (Frontend lint/typecheck/test/build are not on the P0 parity critical path and were not re-run as part of P0.)

### Parity gates

```bash
cd /Users/arunkpatra/codebase/pv_layout_project/python/pvlayout_engine
uv run pytest tests/parity/test_p00_bundled_mst_parity.py -v
```

Expected:
- `test_p00_counts_match_legacy[phaseboundary2]` PASS
- `test_p00_total_dc_matches_legacy[phaseboundary2]` PASS
- `test_p00_total_ac_matches_legacy_modulo_pattern_v[phaseboundary2]` PASS — delta within ±200m of expected −613.5m (legacy 12,974.5m → new ~12,361.0m; Pattern V re-routes 15 boundary-violation cables inside polygon)

---

## 4. Visual side-by-side

Ran live by Arun on 2026-04-29: legacy app and new app open simultaneously, same `phaseboundary2.kmz`. AC cables draw as MST tree per legacy. Pattern V's 15 cables remain a documented divergence (Finding 001) — not a regression. Approved verbally 2026-04-29.

Visual gate on `complex-plant-layout` deferred (see §5).

---

## 5. Acceptance checklist

- [x] Sidecar pytest: 72 passed, 6 skipped, 0 failed
- [x] Parity tests pass on phaseboundary2 (3/3): counts exact, DC total ±0.1m, AC total within ±200m of −613.5m calibrated delta
- [x] Visual side-by-side on phaseboundary2: approved by Arun (verbal, 2026-04-29)
- [—] Visual side-by-side on complex-plant-layout: deferred — capture not feasible at this baseline (see §5); carries to P2+
- [x] Three discovery memos pre-filed: 001 Pattern V, 002 search-space caps, 003 route_quality field
- [—] Prasanta acknowledges memos: async via Arun's daily comms; explicitly NOT gating P1 entry per PLAN.md §5 P0 Gate
- [x] Arun signoff: verbal, 2026-04-29

(`[x]` = done; `[—]` = deferred / not-gating; `[ ]` = pending)

---

## 6. Known limitations / divergences

**Pattern V re-routes 15 AC cables on phaseboundary2** inside the polygon — legacy routes them outside via Pattern F. Net AC delta: ~−613.5m (legacy 12,974.5m → new ~12,361.0m). Calibrated value lives in `PATTERN_V_AC_DELTA_M` in the test file. Documented in Finding 001.

**Task 3 deferred — legacy GUI screenshots + KMZ/PDF/DXF exports** not captured. Not on the P0 critical path; visual gate ran interactively. Future uses (P3 visual rendering gate, P4 export-wiring reference) will require capture. `complex-plant-layout` legacy capture also deferred: capture attempt on 2026-04-29 killed after >20 min wall-clock without completing — legacy at this baseline lacks S11.5 search-space caps, so per-cable AC quantity routing in `_calc_individual_ac_total` compounds badly on a large multi-plot KMZ. Both deferrals carry to P2+.

**`route_quality` field** has no frontend visual treatment — that's P3.

**Search-space caps** may be over-restrictive post-bundling/MST. Documented in Finding 002. Re-evaluation deferred to discussion outcome / P1 instrumentation.

**Cumulative drift beyond P0:** P0 ports only the four cable functions. Wider drift (LA placement changes, layout-engine adjustments, KMZ-parser water-body autodetection, energy-model upgrades, `satellite_water_detector.py`, `tracker_layout_engine.py`, etc.) is enumerated in `docs/parity/BACKLOG.md` and addressed in subsequent parity spikes.

---

## 7. On sign-off

1. Flip P0 to 🟢 in `docs/parity/PLAN.md` §0 header. (Arun's call — do not auto-apply.)
2. Commit STATUS flip.
3. Start P1 (sync skill + inventory).
