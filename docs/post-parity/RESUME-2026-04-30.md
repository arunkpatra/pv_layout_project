# Resume prompt — post-compaction handoff (2026-04-30)

Copy the block between the `--- BEGIN PROMPT ---` markers as the first
message in the resumed session. Reads in ~2 minutes; gives the
post-compaction model everything needed to continue without re-deriving
state.

The full prompt is also reproduced inline in the current chat. This file
is the durable copy.

---

--- BEGIN PROMPT ---

# Resuming post-parity desktop work — 2026-04-30 mid-block

You're resuming a multi-week post-parity build on the
`pv_layout_project` repo. Context was compacted at this checkpoint.

## Branch state

Working directory: `/Users/arunkpatra/codebase/pv_layout_project`.
Branch: `post-parity-v1-desktop` (DO NOT push directly to main).

12 commits ahead of where the parity sweep closed. Most recent four:

```
95773b9 docs(plan): F5 — V2 client foundation closed; per-endpoint additions roll into P-rows
836bf83 feat(auth): F6 — KMZ + run-result S3 upload helpers (B6 / B7 wired)
baaef7f feat(auth): F3 — idempotency-key helper + retry policy for V2 usage/report
8bf5ab4 feat(auth): F2 — V2 entitlements client
```

Plan status: **7 of 52 rows done** in [docs/PLAN.md](docs/PLAN.md).
Done: F1, F4, S1, F2, F3, F6, F5. Next: **P1 (new-project flow)** —
deps F4 + F6 + B11 are all done.

Repo-wide test count: 268 across 3 packages (62 entitlements-client +
28 ui + 178 desktop). All four gates green.

## Locked architectural decisions — DO NOT relitigate

These are settled across F1–F6 and are referenced by [docs/PLAN.md](docs/PLAN.md)'s
Context section. Re-deriving them is wasted work.

- **Cloud-first**: no internet → no app. Project state lives in Postgres
  + S3; local KMZ cache is an input asset only.
- **Tauri Rust shell is the API caller** — but implementation pattern
  is **TS-extension via `@tauri-apps/plugin-http`** (which delegates to
  native Rust HTTP under the hood — no CORS, no preflight, full
  streaming for 50MB blobs). NOT a separate Rust HTTP crate. F5 row
  notes capture the rationale.
- **License-key bearer auth** (`sl_live_*`); never Clerk on the desktop.
- **AWS S3 in `ap-south-1`** (account `378240665051`, IAM user
  `renewable-energy-app`). Bucket family `solarlayout-{env}-projects`.
  Key paths: KMZ → `projects/<userId>/kmz/<sha256>.kmz` (content-
  addressed); Runs → `projects/<userId>/<projectId>/runs/<runId>/...`.
- **V2 envelope**: `{success: true, data: T}` / `{success: false, error:
  {code, message, details?}}`. V1 routes use a looser
  `{error: {message, code?}}` shape; the client tries V2 first, falls
  back to V1.
- **V2ErrorCode union** (locked exhaustive): `UNAUTHORIZED`,
  `VALIDATION_ERROR`, `PAYMENT_REQUIRED`, `CONFLICT`, `NOT_FOUND`,
  `S3_NOT_CONFIGURED`, `INTERNAL_SERVER_ERROR`. `EntitlementsError.code`
  is the typed branch.
- **Project quotas (concurrent)**: Free=3, Basic=5, Pro=10, Pro Plus=15.
  Effective quota = max across active+non-exhausted entitlements.
- **PAYG-only at v1**: $1.99 / $4.99 / $14.99 packs. No subscriptions.
- **Idempotency**: UUID v4 per "Generate Layout" intent, reused across
  retries. Server's `@@unique(userId, idempotencyKey)` dedupes.
- **Wallet model**: cheapest-tier-first that supports the requested
  feature. Already correct in the backend; desktop never re-implements.
- **Run = persisted artifact**: each "Generate Layout" = 1 calc-debit +
  1 Run row. Compare = split-view of 2 runs in same project. Run delete
  does NOT refund the calc.
- **Auto-save**: 2s debounce on `PATCH /v2/projects/:id` (B13).

## Backend status (last known: 2026-04-30)

Branch `post-parity-v2-backend` in `/Users/arunkpatra/codebase/renewable_energy`.

Done: **B1–B16** (schema + idempotency + project/run models + S3 buckets
+ B6 KMZ upload URL + B7 run-result upload URL + B8 V2 entitlements +
B9 idempotent V2 usage/report + B10 GET /v2/projects + B11 POST
/v2/projects + B12–B16 project + run CRUD incl. B16 atomic
debit-and-create-run with embedded upload URL).

Remaining (NOT blocking desktop right now):
- B17 (GET /v2/projects/:id/runs/:runId)
- B18 (DELETE /v2/projects/:id/runs/:runId)
- B19 (Stripe webhook + projectQuota propagation)
- B20 / B21 (V1 deprecation — launch-day chores)

Local mvp_api on `http://localhost:3003`. Desktop's local-dev override
is `VITE_SOLARLAYOUT_API_URL=http://localhost:3003` in
`apps/desktop/.env.local`.

## Test fixture license keys (stable; backend's seed script is idempotent)

Re-seed by running this in the backend repo:
`bun run packages/mvp_db/prisma/seed-desktop-test-fixtures.ts`

| Scenario     | License key                                   | State |
|--------------|-----------------------------------------------|-------|
| FREE         | `sl_live_desktop_test_FREE_stable`            | Free 5/5; 0 projects |
| BASIC        | `sl_live_desktop_test_BASIC_stable`           | Free 0/5 + Basic 5/5; 0 projects |
| PRO          | `sl_live_desktop_test_PRO_stable`             | Free 0/5 + Pro 10/10; 0 projects |
| PRO_PLUS     | `sl_live_desktop_test_PRO_PLUS_stable`        | Free 0/5 + Pro Plus 50/50; 1 Project + 1 Run for B7 fixture |
| MULTI        | `sl_live_desktop_test_MULTI_stable`           | Free 3/5 + Pro 8/10 (cheapest-first wallet test) |
| EXHAUSTED    | `sl_live_desktop_test_EXHAUSTED_stable`       | Free 0/5 + Pro 0/10 (all maxed) |
| DEACTIVATED  | `sl_live_desktop_test_DEACTIVATED_stable`     | Pro entitlement deactivated; B8 returns 0 quota |
| QUOTA_EDGE   | `sl_live_desktop_test_QUOTA_EDGE_stable`      | Free 5/5 + 3 active projects (AT Free quota) |

B7 fixture IDs (PRO_PLUS owns these — for testing run-result uploads
without B16):
- `projectId = prj_b7fixturePROPLUS00000000000000000000`
- `runId = run_b7fixturePROPLUS00000000000000000000`

## Key files for re-reading

- [docs/PLAN.md](docs/PLAN.md) — active backlog. Re-read the Context
  section + the F-rows' notes if any architectural detail is unclear.
- [packages/entitlements-client/src/types-v2.ts](packages/entitlements-client/src/types-v2.ts) — V2 wire-shape mirror.
  Header documents canonical-source path in renewable_energy and the
  lockstep update obligation.
- [packages/entitlements-client/src/client.ts](packages/entitlements-client/src/client.ts) — V2 client methods
  (B6/B7/B8/B9 done; B10–B18 not yet added — accrete in P-rows).
- [apps/desktop/src/auth/idempotency.ts](apps/desktop/src/auth/idempotency.ts) — UUID v4 + transient-retry.
- [apps/desktop/src/auth/s3upload.ts](apps/desktop/src/auth/s3upload.ts) — `putToS3` + `uploadKmzToS3`
  + `uploadRunResultToS3` orchestrators.
- [apps/desktop/src/auth/useEntitlements.ts](apps/desktop/src/auth/useEntitlements.ts) — V2 entitlements hook.
- [apps/desktop/src/auth/useReportUsage.ts](apps/desktop/src/auth/useReportUsage.ts) — V2 usage report hook
  with idempotency + cache hydration.
- [apps/desktop/src/state/project.ts](apps/desktop/src/state/project.ts) — F4 Zustand state
  (currentProject + runs + selectedRunId).
- [docs/post-parity/discovery/](docs/post-parity/discovery/) — five
  audits (legacy capability, V1 backend, infra, codebase, planning).

## Next concrete step: P1 — New-project flow

Plan row: P1 (T2). Deps F4 ✓ + F6 ✓ + B11 ✓.

Acceptance: "+ New project" button → file picker → KMZ uploaded via
F6 (uploadKmzToS3) → backend creates project (B11) → opens in current
tab. Quota check: 402 from B11 → show upsell modal.

Likely files to touch:
- `apps/desktop/src/App.tsx` (or a new project-creation component)
- `apps/desktop/src/state/project.ts` (set currentProject after create)
- `packages/entitlements-client/src/client.ts` — ADD `createProjectV2`
  method (B11). Per F5's scope clarification, this is the right row to
  add it.
- New TanStack Query mutation hook similar to `useReportUsageMutation`.
- Tests + manual runtime verification when the flow is in place.

## Pending coordination

After P1 ships, run a real-time fixture session with the backend
session: drive the desktop through all 8 fixture scenarios end-to-end
while the backend session watches mvp_api logs. The PRO_PLUS fixture
project + run lets B7 round-trip without B16. The QUOTA_EDGE fixture
exercises P1's 402 path. EXHAUSTED exercises usage-report 402 in P6.

This is the F6 deferral ("end-to-end runtime verification") plus broader
coverage. Schedule when both sessions are in a good place to focus.

## Process discipline reminders

- TDD-first per row: failing test → minimal impl → green → commit.
- Atomic commit per row: `feat(scope): description`. Intra-row checkpoints
  use `wip:` and squash at row close.
- Pre-commit gate from repo root: `bun run lint && bun run typecheck && bun run test && bun run build`. Skip Python/sidecar pytest unless
  touching `python/pvlayout_engine`.
- Mark row done in PLAN.md in the same commit that closes it; bump the
  status counter.
- Use TodoWrite for multi-step rows.
- F2-F6 pattern: V2 client method + envelope handling + tests + hook +
  tests + cache hydration. P1 follows the same pattern; the hook is the
  natural unit per row.

## What I'm carrying that survived compaction

The current chat may have been compacted; the auto-memory system
(`~/.claude/projects/-Users-arunkpatra-codebase-pv-layout-project/memory/`)
should still have:
- User working style (recommend-don't-ask, scope-tight, no narration)
- Handoff format
- Planning artifact density
- External contracts process
- Bit-exact parity assertions
- Post-parity directive (Prasanta's 3 principles)
- Co-founder partnership
- Wire-schema passthrough check
- uv sync --extra dev requirement

If MEMORY.md is missing entries that look obviously needed, ping me and
we'll re-derive.

## Standing by

Read this prompt + scan the four most recent commits + docs/PLAN.md's
F-rows + Context section. Confirm understanding, then proceed with P1
(or pause if I want to re-coordinate with the backend first).

--- END PROMPT ---
