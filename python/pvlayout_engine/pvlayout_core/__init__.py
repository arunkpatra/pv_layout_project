"""
pvlayout_core — SolarLayout domain logic.

Vendored verbatim from PVlayout_Advance/{core,models,utils} at S1.
Do not modify these modules; the sidecar (pvlayout_engine) wraps them
with HTTP endpoints without changing their behavior.

Subpackages:
  pvlayout_core.core     — layout engine, parsers, exporters, edition flags
  pvlayout_core.models   — dataclasses (LayoutParameters, LayoutResult, …)
  pvlayout_core.utils    — geo helpers (UTM/WGS84)
"""
